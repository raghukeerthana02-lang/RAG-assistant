# Secrets -- never bake these into an image layer
.env
.env.example

# Unrelated app -- the frontend is deployed separately
frontend

# Local dev artifacts, not needed in the image
.git
.gitignore
__pycache__
*.pyc
.vite
faiss_store
test_env
eval

# Docs, not needed at runtime
README.md
supabase_rls.sql
