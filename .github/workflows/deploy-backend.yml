name: Deploy RAG Assistant Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v4


    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}


    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/rag-assistant-backend:latest .


    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/rag-assistant-backend:latest


    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu
        key: ${{ secrets.EC2_SSH_KEY }}

        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/rag-assistant-backend:latest

          docker stop rag-backend || true

          docker rm rag-backend || true

          docker run -d \
            --name rag-backend \
            -p 8000:8000 \
            --env-file /home/ubuntu/.env \
            ${{ secrets.DOCKER_USERNAME }}/rag-assistant-backend:latest