fastapi==0.141.1
uvicorn==0.42.0
python-multipart==0.0.22
python-dotenv==1.2.2
pydantic==2.13.4

supabase==2.31.0

faiss-cpu==1.14.3
sentence-transformers==5.4.0
rank-bm25==0.2.2
nltk==3.10.1
numpy==2.4.2

pypdf==6.14.2
python-docx==1.2.0
python-pptx==1.0.2

groq==1.6.0
slowapi==0.1.10

# python-magic-bin bundles libmagic for Windows dev; on Linux (deploy
# targets) use python-magic against the system libmagic1 package instead.
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"

sentry-sdk==2.68.0

# Only needed if REDIS_URL is set (shares rate-limit counters across
# multiple worker processes/instances); unused otherwise.
redis==5.2.1
